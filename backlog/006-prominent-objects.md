# prominent-objects: Which objects are prominent, for flight visibility

<!-- rules-factory-entry: prominent-objects -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->

Backlog item 6 of 37 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `prominent-objects`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.51(c)
```

Evidence, verbatim:

```text
For purposes of this section, flight visibility means the average slant distance from the control station at which prominent unlighted objects may be seen and identified by day and prominent lighted objects may be seen and identified by night.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
Part 107 does not define 'prominent', and the term carries the whole visibility metric: the distance reported as flight visibility is the distance at which prominent objects can be seen, so the degree of prominence fixes the number that § 107.51(c)'s 3-statute-mile threshold is compared against.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.ProminentObjects`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `prominent-objects` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Gate 1 of 0008 passes: applying § 107.51(c) requires fixing a degree the corpus did not fix, which is what separates flight visibility from groundspeed — an ordinary caller-supplied parameter whose meaning is not open. Gate 2 does not fire: no corpus is named for it. Gate 3's first branch does not fire either — the clause states no measure for 'prominent' in its own constituent and names no decider — so the procedure returns a gap. The same shape as well-clear and moving-vehicle-operation's 'sparsely populated area'. Found by the undefined-term sweep (#33, #34), not by inspection of the interesting entries.
```
