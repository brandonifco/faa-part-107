# intensity-reduction-in-interest-of-safety: Reducing the anti-collision lighting intensity is in the interest of safety

<!-- rules-factory-entry: intensity-reduction-in-interest-of-safety -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 18 of 31 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `intensity-reduction-in-interest-of-safety`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.29(a)(2), (b)
```

Evidence, verbatim:

```text
The remote pilot in command may reduce the intensity of, but may not extinguish, the anti-collision lighting if he or she determines that, because of operating conditions, it would be in the interest of safety to do so.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.IntensityReductionInInterestOfSafety`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `intensity-reduction-in-interest-of-safety` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The corpus delegates the judgement and names its decider in the same breath: "if he or she determines". § 107.29(a)(2) and (b) state the identical sentence, which is why the locator cites both and why the quoted evidence has two occurrences. The computable half stays where it was: the bound that intensity may be reduced but the lighting may not be extinguished is anti-collision-lighting's, and that entry depends on this one — the speed-limit / speed-within-limit shape 0005 names. This is the example method.md and corpus-map.md both cite for kind: assertion, and it had no entry of its own until #26.
```
