# altitude-limit: Maximum altitude above ground level

<!-- rules-factory-entry: altitude-limit -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->

Backlog item 1 of 37 for FaaPart107, from RulesFactory.Maps.FaaPart107 3.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `altitude-limit`
- kind: `value`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.51(b)
```

Evidence, verbatim:

```text
(b) The altitude of the small unmanned aircraft cannot be higher than 400 feet above ground level, unless the small unmanned aircraft: (1) Is flown within a 400-foot radius of a structure; and (2) Does not fly higher than 400 feet above the structure's immediate uppermost limit.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine states this value as the evidence prints it -- every figure and unit, and where the corpus prints a finite table, the whole table.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.AltitudeLimit`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `altitude-limit` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
400 feet, and the 400-foot structure radius and 400-foot above-structure allowance as three distinct figures.
```
