# night-training-completed: The remote pilot in command has completed the knowledge test or training for night operation

<!-- rules-factory-entry: night-training-completed -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->

Backlog item 14 of 37 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `night-training-completed`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.29(a)(1)
```

Evidence, verbatim:

```text
(1) The remote pilot in command of the small unmanned aircraft has completed an initial knowledge test or training, as applicable, under § 107.65 after April 6, 2021; and
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
Whether "initial" qualifies "training" as well as "knowledge test" is not settled, and "as applicable, under § 107.65" points at a section that names an initial aeronautical knowledge test (a), recurrent training (b) and training for part 61 certificate holders (c) without saying which satisfies § 107.29(a)(1); § 107.65(d) also treats some completions before April 6, 2021 as compliant with (b) or (c).
```

## Dependencies

`dependsOn` -- built before this item:

- `knowledge-recency` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "under § 107.65" -- `resolvedBy` `knowledge-recency` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.NightTrainingCompleted`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `night-training-completed` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The completion date against April 6, 2021 is computable once the qualifying completion is known; which completion qualifies is not. Split out of night-operation, which called the training condition computable, by the blind second mapping (blind-mapping/ (0014)).
```
