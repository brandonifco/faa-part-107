# well-clear: The small unmanned aircraft is well clear of the aircraft or vehicle

<!-- rules-factory-entry: well-clear -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 14 of 34 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `well-clear`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.37(a)
```

Evidence, verbatim:

```text
Yielding the right of way means that the small unmanned aircraft must give way to the aircraft or vehicle and may not pass over, under, or ahead of it unless well clear.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
Part 107 does not define 'well clear', and the term carries the whole exception to the prohibition on passing over, under or ahead. 'Well clear' occurs exactly once in the corpus; § 107.3 does not define it and no other section does; and § 107.37(a) states no measure for it in its own constituent — 'must give way' is a coordinate obligation in the definiens, not what the clearance is measured against.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.WellClear`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `well-clear` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Split out of right-of-way, which keeps the two computable enumerations (0005: a standard is its own entry, and the rule that consumes it depends on it). Not kind: assertion — gate 3's first branch requires the corpus to state, in the same constituent as the open term, either the measure or the set of values, and § 107.37(a) states neither. Not a value either: the corpus states no clearance. The kind records what the engine would have to do and cannot, which is why the entry declines at runtime rather than demanding a parameter. The same shape as moving-vehicle-operation's 'sparsely populated area', and the two now return the same answer, which they did not before. Where the project holds the term determinate — 'well clear' is a recognised operational standard in aviation practice — that is fate: decision with a record, never a kind (0008 § 3); this map does not hold it, so the fate is unresolved.
```
