# collision-hazard-proximity: Operating so close to another aircraft as to create a collision hazard

<!-- rules-factory-entry: collision-hazard-proximity -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 13 of 31 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `collision-hazard-proximity`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.37(b)
```

Evidence, verbatim:

```text
(b) No person may operate a small unmanned aircraft so close to another aircraft as to create a collision hazard.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A hand-written handler implements `Handlers.CollisionHazardProximity`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `collision-hazard-proximity` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The corpus states a standard rather than a distance, deliberately, and § 107.37(b) is that sentence and nothing else — so the entry is reclassified in place rather than split. The assertion is asserted and withheld, attributed and recorded with the outcome, and never defaulted to true when absent. An engine that resolved it to a number would substitute its own rule for the corpus's. Who asserts it: § 107.37(b) names only the subject of the prohibition, "No person", and nobody whose determination settles the hazard, so assertedBy is the caller (0025).
```
