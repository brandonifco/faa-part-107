# effective-communication: Effective communication is maintained among the three named persons

<!-- rules-factory-entry: effective-communication -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 24 of 32 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `effective-communication`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.33(a)
```

Evidence, verbatim:

```text
(a) The remote pilot in command, the person manipulating the flight controls of the small unmanned aircraft system, and the visual observer must maintain effective communication with each other at all times.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
Part 107 does not define "effective", and the term carries the whole of § 107.33(a). The paragraph states no measure for it in its own constituent — "with each other at all times" fixes between whom and when, not what effectiveness is measured against — and vests the determination in nobody: it is an obligation on three named persons, not a judgement the corpus gives them.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.EffectiveCommunication`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `effective-communication` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Gate 1 passes: applying the paragraph requires fixing a degree the corpus did not fix. Gate 2 does not fire — § 107.3 does not define the term and no corpus is named for it. Gate 3's first arm does not fire, so the procedure returns a gap: the same shape as well-clear, prominent-objects and moving-vehicle-operation's "sparsely populated area". That the operator is the only person who could answer the question is not what gate 3 asks. An operator who asserts that communication was effective has asserted a conclusion the engine cannot check and that the corpus gave it no warrant to delegate; 0008 § 2 rules that where a term's determinacy rests on knowledge outside the corpus, that belief is fate: decision with a record, never a kind. Split out of visual-observer-conditions (0010).
```
