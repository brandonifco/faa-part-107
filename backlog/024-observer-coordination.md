# observer-coordination: The three named persons coordinate to scan the airspace and maintain awareness

<!-- rules-factory-entry: observer-coordination -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 24 of 31 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `observer-coordination`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.33(c)
```

Evidence, verbatim:

```text
(c) The remote pilot in command, the person manipulating the flight controls of the small unmanned aircraft system, and the visual observer must coordinate to do the following: (1) Scan the airspace where the small unmanned aircraft is operating for any potential collision hazard; and (2) Maintain awareness of the position of the small unmanned aircraft through direct visual observation.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.ObserverCoordination`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `observer-coordination` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Gate 3's first arm fires: "must coordinate" is followed in the same constituent by "to do the following:" and a closed two-item enumeration — scan the airspace for any potential collision hazard, and maintain awareness of the position through direct visual observation. The infinitival adjunct states what the coordination is for exactly as "to avoid a collision" states what the flash rate is for. "Maintain awareness" is inside that enumeration and is not a second open term, and "through direct visual observation" fixes its means. The assertion is asserted and withheld, attributed to the three named persons and recorded with the outcome, and never defaulted to true when absent. Split out of visual-observer-conditions (0010).
```
