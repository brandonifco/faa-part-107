# speed-limit: Maximum groundspeed

<!-- rules-factory-entry: speed-limit -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->

Backlog item 1 of 39 for FaaPart107, from RulesFactory.Maps.FaaPart107 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `speed-limit`
- kind: `value`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.51(a)
```

Evidence, verbatim:

```text
(a) The groundspeed of the small unmanned aircraft may not exceed 87 knots (100 miles per hour).
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
The limit is printed in two units that are not equal — 87 knots is 100.12 miles per hour — and the corpus does not say which governs, so a groundspeed above 100 mph and at or below 87 knots both exceeds and does not exceed it.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine states this value as the evidence prints it -- every figure and unit, and where the corpus prints a finite table, the whole table.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.SpeedLimit`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `speed-limit` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The single figure, both units as printed: 87 knots / 100 mph. They differ by about 0.12 mph, and nothing in part 107 says the parenthetical is a rounding the knots figure overrides; that question is recorded rather than decided. Found by the blind second mapping (blind-mapping/ (0014)).
```
