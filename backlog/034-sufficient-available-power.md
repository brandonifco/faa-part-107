# sufficient-available-power: There is enough available power for the intended operational time

<!-- rules-factory-entry: sufficient-available-power -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->

Backlog item 34 of 37 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `sufficient-available-power`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.49(d)
```

Evidence, verbatim:

```text
(d) If the small unmanned aircraft is powered, ensure that there is enough available power for the small unmanned aircraft system to operate for the intended operational time;
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A hand-written handler implements `Handlers.SufficientAvailablePower`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `sufficient-available-power` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
"Enough available power" is followed in its own constituent by "for the small unmanned aircraft system to operate for the intended operational time" — an infinitival adjunct on enough, the same construction as "a flash rate sufficient to avoid a collision". Gate 3's first arm fires. The intended operational time is an ordinary caller-supplied parameter that gate 1 stops and that gets no entry of its own, as corpus-map.md rules for airspace-authorized's airspace class. Split out of preflight-actions (0010). Who asserts it is named in § 107.49's lead-in, "Prior to flight, the remote pilot in command must:" (assertedBy, 0025).
```
