# preflight-risk-assessment: The operating environment has been assessed against the stated risks

<!-- rules-factory-entry: preflight-risk-assessment -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 20 of 24 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `preflight-risk-assessment`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.49(a)
```

Evidence, verbatim:

```text
(a) Assess the operating environment, considering risks to persons and property in the immediate vicinity both on the surface and in the air. This assessment must include: (1) Local weather conditions; (2) Local airspace and any flight restrictions; (3) The location of persons and property on the surface; and (4) Other ground hazards.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A hand-written handler implements `Handlers.PreflightRiskAssessment`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `preflight-risk-assessment` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Gate 3's first arm fires twice over: "Assess the operating environment" is qualified in its own constituent by "considering risks to persons and property in the immediate vicinity both on the surface and in the air" — what the assessment is measured against — and then by a closed four-item list of what it must include. The bearer is named in the section's lead-in, "Prior to flight, the remote pilot in command must:", and is who asserts it (assertedBy, 0025). The limit, stated where the claim is made: the measure itself carries an open degree, "immediate vicinity", and gate 3 does not ask whether a stated measure is itself determinate. A measure that needs a measure is a shape 0008's procedure cannot see; 0010 records it rather than deciding it.
```
