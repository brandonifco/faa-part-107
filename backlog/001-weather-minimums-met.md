# weather-minimums-met: Visibility and cloud clearance are met

<!-- rules-factory-entry: weather-minimums-met -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 1 of 21 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `weather-minimums-met`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.51(c)-(d)
```

Evidence, verbatim:

```text
(c) The minimum flight visibility, as observed from the location of the control station must be no less than 3 statute miles. For purposes of this section, flight visibility means the average slant distance from the control station at which prominent unlighted objects may be seen and identified by day and prominent lighted objects may be seen and identified by night. (d) The minimum distance of the small unmanned aircraft from clouds must be no less than: (1) 500 feet below the cloud; and (2) 2,000 feet horizontally from the cloud.
```

## Dependencies

`dependsOn` -- built before this item:

- `visibility-minimum` -- no backlog item (status: implemented)
- `cloud-clearance` -- no backlog item (status: implemented)
- `prominent-objects` -- no backlog item (status: implemented)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.WeatherMinimumsMet`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `weather-minimums-met` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Each minimum independently, and the case where one passes and the other fails. The cloud-clearance half has two stated figures, but whether both must hold at once or either suffices is cloud-clearance's open question. The visibility half is not: the quantity to be compared against 3 statute miles is the section's own 'flight visibility', whose definiens turns on which objects are 'prominent', and the corpus fixes no degree of prominence. That term is prominent-objects, split out rather than reclassifying this entry — classifying the whole entry by one of its clauses would have thrown away the 500-foot and 2,000-foot figures (0005).
```
