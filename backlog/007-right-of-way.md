# right-of-way: Yielding right of way to other aircraft

<!-- rules-factory-entry: right-of-way -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: blocked -->

Backlog item 7 of 25 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `right-of-way`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.37(a)
```

Evidence, verbatim:

```text
(a) Each small unmanned aircraft must yield the right of way to all aircraft, airborne vehicles, and launch and reentry vehicles. Yielding the right of way means that the small unmanned aircraft must give way to the aircraft or vehicle and may not pass over, under, or ahead of it unless well clear.
```

## Dependencies

`dependsOn` -- built before this item:

- [`well-clear`](006-well-clear.md)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.RightOfWay`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `right-of-way` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Two enumerations, both computable: three protected kinds — aircraft, airborne vehicles, launch and reentry vehicles — and three prohibited relative positions — over, under, ahead. An engine told the object is none of the three, or that the manoeuvre is none of the three, decides the case without the open term at all, and the evidence covers each. The note this replaces said "nothing here is computed", which its own evidence contradicts (#34). What is not computed is the exception "unless well clear", which is well-clear and is a gap rather than an assertion: 0008 § 2 attacked the purpose clause at this joint and it gave way, so gate 3's first branch does not fire here.
```
