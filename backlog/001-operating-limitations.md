# operating-limitations: The operating limitations are complied with

<!-- rules-factory-entry: operating-limitations -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 1 of 11 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `operating-limitations`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.51 introductory text
```

Evidence, verbatim:

```text
A remote pilot in command and the person manipulating the flight controls of the small unmanned aircraft system must comply with all of the following operating limitations when operating a small unmanned aircraft system:
```

## Dependencies

`dependsOn` -- built before this item:

- `speed-within-limit` -- no backlog item (status: implemented)
- `altitude-within-limit` -- no backlog item (status: implemented)
- `weather-minimums-met` -- no backlog item (status: implemented)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.OperatingLimitations`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `operating-limitations` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The lead-in of § 107.51: the four limitations are one obligation, and it binds both the remote pilot in command and the person manipulating the flight controls. No entry quoted it until the blind second mapping (blind-mapping/ (0014)). Cases: all limitations met; each one failing; and each of the two named persons.
```
