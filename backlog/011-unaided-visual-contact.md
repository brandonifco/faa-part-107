# unaided-visual-contact: The aircraft can be seen unaided throughout the entire flight, for the four stated purposes

<!-- rules-factory-entry: unaided-visual-contact -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 11 of 16 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `unaided-visual-contact`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.31(a)
```

Evidence, verbatim:

```text
(a) With vision that is unaided by any device other than corrective lenses, the remote pilot in command, the visual observer (if one is used), and the person manipulating the flight control of the small unmanned aircraft system must be able to see the unmanned aircraft throughout the entire flight in order to: (1) Know the unmanned aircraft's location; (2) Determine the unmanned aircraft's attitude, altitude, and direction of flight; (3) Observe the airspace for other air traffic or hazards; and (4) Determine that the unmanned aircraft does not endanger the life or property of another.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.UnaidedVisualContact`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `unaided-visual-contact` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Gate 3's first arm fires on the plainest measure in this corpus. § 107.31(a) states an open ability — "must be able to see the unmanned aircraft throughout the entire flight" — and fixes what it is measured against in the same constituent, with "in order to:" and four enumerated purposes. That is the "sufficient to avoid a collision" construction with four adjuncts instead of one. § 107.31(a)(4)'s "endanger the life or property of another" sits inside that measure and is not a second open term: § 107.31 does not prohibit endangering, it requires the ability to see well enough to determine. The assertion is asserted and withheld for each named person, attributed and recorded with the outcome, and never defaulted to true when absent; the corpus states no distance, so no figure is evidenced. Split out of visual-line-of-sight, which keeps § 107.31(b)'s two permitted combinations — 0005's rule that a standard is its own entry and the rule that consumes it depends on it. Decided in 0010.
```
