# attached-object-no-adverse-effect: An attached or carried object does not adversely affect flight characteristics or controllability

<!-- rules-factory-entry: attached-object-no-adverse-effect -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 33 of 34 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `attached-object-no-adverse-effect`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.49(e)
```

Evidence, verbatim:

```text
does not adversely affect the flight characteristics or controllability of the aircraft
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A hand-written handler implements `Handlers.AttachedObjectNoAdverseEffect`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `attached-object-no-adverse-effect` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
"Adversely affect" states what it is measured against in its own constituent and enumerates it: the flight characteristics or controllability of the aircraft. Gate 3's first arm fires. Split from attached-object-secure, whose conjunct states no measure (0010). Who asserts it is named in § 107.49's lead-in, "Prior to flight, the remote pilot in command must:" (assertedBy, 0025).
```
