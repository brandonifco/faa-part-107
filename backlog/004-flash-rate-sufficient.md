# flash-rate-sufficient: The anti-collision lighting has a flash rate sufficient to avoid a collision

<!-- rules-factory-entry: flash-rate-sufficient -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: ready -->

Backlog item 4 of 11 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `flash-rate-sufficient`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.29(a)(2), (b)
```

Evidence, verbatim:

```text
(2) The small unmanned aircraft has lighted anti-collision lighting visible for at least 3 statute miles that has a flash rate sufficient to avoid a collision. ... (b) No person may operate a small unmanned aircraft system during periods of civil twilight unless the small unmanned aircraft has lighted anti-collision lighting visible for at least 3 statute miles that has a flash rate sufficient to avoid a collision.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.FlashRateSufficient`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `flash-rate-sufficient` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
The same clause qualifies the night requirement in (a)(2) and the civil-twilight requirement in (b), which is why it hangs off anti-collision-lighting — the entry whose locator cites both — rather than off night-operation alone. The assertion is asserted and withheld, attributed and recorded with the outcome, and never defaulted to true when absent; the corpus states no rate, so no figure is evidenced. Added by Amendment 107-8; the 2020 text states no flash-rate requirement anywhere. Who asserts it: § 107.29(a) and (b) name the subject of the prohibition, "no person may operate", and nobody whose determination settles the flash rate; the remote pilot in command they name determines only whether to reduce the lighting's intensity, which is intensity-reduction-in-interest-of-safety. So assertedBy is the caller (0025).
```
