# civil-twilight-operation: Operation during civil twilight

<!-- rules-factory-entry: civil-twilight-operation -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: blocked -->

Backlog item 7 of 11 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `civil-twilight-operation`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.29(b)-(c)
```

Evidence, verbatim:

```text
(b) No person may operate a small unmanned aircraft system during periods of civil twilight unless the small unmanned aircraft has lighted anti-collision lighting visible for at least 3 statute miles that has a flash rate sufficient to avoid a collision. The remote pilot in command may reduce the intensity of, but may not extinguish, the anti-collision lighting if he or she determines that, because of operating conditions, it would be in the interest of safety to do so. (c) For purposes of paragraph (b) of this section, civil twilight refers to the following: (1) Except for Alaska, a period of time that begins 30 minutes before official sunrise and ends at official sunrise; (2) Except for Alaska, a period of time that begins at official sunset and ends 30 minutes after official sunset; and (3) In Alaska, the period of civil twilight as defined in the Air Almanac.
```

## Dependencies

`dependsOn` -- built before this item:

- `civil-twilight-window` -- no backlog item (status: implemented)
- `civil-twilight-alaska` -- no backlog item (definedElsewhere -- the corpus does not supply the fact)
- [`anti-collision-lighting`](006-anti-collision-lighting.md)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "paragraph (b) of this section" -- `resolvedBy` [`anti-collision-lighting`](006-anti-collision-lighting.md)
- `cites` "as defined in the Air Almanac" -- `resolvedBy` `civil-twilight-alaska` -- no backlog item (definedElsewhere -- the corpus does not supply the fact)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.CivilTwilightOperation`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `civil-twilight-operation` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Inside and outside each twilight window, with and without lighting. Alaska is a separate entry: the corpus defers the definition elsewhere. It stays in dependsOn because (c)(3) is part of the definition of civil twilight this entry's rule turns on: without civil-twilight-alaska the entry cannot say whether an Alaskan operation is in civil twilight (blind-mapping/ (0014)). (c)'s "paragraph (b) of this section" resolves to anti-collision-lighting, whose locator cites § 107.29(a)(2) and (b) and whose evidence quotes all of (b) as amended by Amendment 107-8, both sentences; flash-rate-sufficient and intensity-reduction-in-interest-of-safety each quote one sentence of it. The pointer resolves to where (b) is stated; the rule that uses (c)'s definition is this entry's, and anti-collision-lighting does not depend on it. It was unmapped, with a reason saying no other entry states (b) whole, which was not true.
```
