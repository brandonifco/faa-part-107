# visual-observer-conditions: Visual observer requirements, when one is used

<!-- rules-factory-entry: visual-observer-conditions -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: blocked -->

Backlog item 14 of 18 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `visual-observer-conditions`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.33
```

Evidence, verbatim:

```text
If a visual observer is used during the aircraft operation, all of the following requirements must be met: (a) The remote pilot in command, the person manipulating the flight controls of the small unmanned aircraft system, and the visual observer must maintain effective communication with each other at all times. (b) The remote pilot in command must ensure that the visual observer is able to see the unmanned aircraft in the manner specified in § 107.31. (c) The remote pilot in command, the person manipulating the flight controls of the small unmanned aircraft system, and the visual observer must coordinate to do the following: (1) Scan the airspace where the small unmanned aircraft is operating for any potential collision hazard; and (2) Maintain awareness of the position of the small unmanned aircraft through direct visual observation.
```

## Dependencies

`dependsOn` -- built before this item:

- [`visual-line-of-sight`](012-visual-line-of-sight.md)
- `effective-communication` -- no backlog item (status: implemented)
- [`observer-coordination`](013-observer-coordination.md)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- `cites` "in the manner specified in § 107.31" -- `resolvedBy` [`visual-line-of-sight`](012-visual-line-of-sight.md)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.VisualObserverConditions`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `visual-observer-conditions` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
What survives the split is (b) and the condition of application: if a visual observer is used, the remote pilot in command must ensure the observer can see the aircraft in the manner § 107.31 specifies — computable once unaided-visual-contact is asserted, reached through visual-line-of-sight. (a) is effective-communication, a gap; (c) is observer-coordination, an assertion. The section's three requirements do not share one verdict, which is what 0010 decides. § 107.33(b)'s "in the manner specified in § 107.31" is declared in crossReferences and resolves to visual-line-of-sight, the entry that states § 107.31 whole; it is the same edge as dependsOn. An earlier sentence here said the words were outside check-map.py's pointer list and nothing demanded a declaration; the manifest's pointerPhrases now detect "§ 107.31" (0026), so that was no longer true.
```
