# cloud-clearance: Minimum distance from clouds

<!-- rules-factory-entry: cloud-clearance -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->

Backlog item 3 of 37 for FaaPart107, from RulesFactory.Maps.FaaPart107 3.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `cloud-clearance`
- kind: `value`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.51(d)
```

Evidence, verbatim:

```text
(d) The minimum distance of the small unmanned aircraft from clouds must be no less than: (1) 500 feet below the cloud; and (2) 2,000 feet horizontally from the cloud.
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
The two minimums are joined by "and" and no clearance above a cloud is stated. Read literally, the aircraft must be both at least 500 feet below and at least 2,000 feet horizontally from a cloud, which would bar flight level with or above a distant cloud; read as a region around the cloud, either distance suffices. The corpus does not say which.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine states this value as the evidence prints it -- every figure and unit, and where the corpus prints a finite table, the whole table.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.CloudClearance`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `cloud-clearance` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Both figures: 500 ft below, 2,000 ft horizontal. A single combined value would lose the distinction. The figures are fixed; how they combine is not. Found by the blind second mapping (blind-mapping/ (0014)).
```
