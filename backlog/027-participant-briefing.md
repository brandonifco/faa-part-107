# participant-briefing: The directly participating persons have been informed of the five stated matters

<!-- rules-factory-entry: participant-briefing -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->
<!-- rules-factory-state: blocked -->

Backlog item 27 of 31 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `participant-briefing`
- kind: `assertion`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.49(b)
```

Evidence, verbatim:

```text
(b) Ensure that all persons directly participating in the small unmanned aircraft operation are informed about the operating conditions, emergency procedures, contingency procedures, roles and responsibilities, and potential hazards;
```

## Dependencies

`dependsOn` -- built before this item:

- [`direct-participation`](015-direct-participation.md)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Cross-references

`crossReferences` -- each pointer the evidence makes, and the entry or the recorded reason it resolves to:

- none

## Acceptance criteria

- [ ] The engine demands this fact of the caller rather than deciding it; what the caller states is what the engine evaluates.
- [ ] A hand-written handler implements `Handlers.ParticipantBriefing`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `participant-briefing` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
"Informed about" is fixed by the set stated in the same constituent — operating conditions, emergency procedures, contingency procedures, roles and responsibilities, and potential hazards — which is gate 3's second arm, the delegated choice among values the corpus fixes ("either thrice or four times"), and the set is quotable from this entry's own evidence. The audience is not fixed: "all persons directly participating" is the same undefined term as § 107.39(a), which is direct-participation, a gap, and this entry depends on it. One paragraph, two open terms, two verdicts — which is the sharpest instance of what 0010 decides. Who asserts it is named in § 107.49's lead-in, "Prior to flight, the remote pilot in command must:" (assertedBy, 0025).
```
