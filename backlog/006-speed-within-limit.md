# speed-within-limit: Groundspeed is within the limit

<!-- rules-factory-entry: speed-within-limit -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->

Backlog item 6 of 39 for FaaPart107, from RulesFactory.Maps.FaaPart107 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `speed-within-limit`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.51(a)
```

Evidence, verbatim:

```text
(a) The groundspeed of the small unmanned aircraft may not exceed 87 knots (100 miles per hour).
```

## Dependencies

`dependsOn` -- built before this item:

- [`speed-limit`](001-speed-limit.md)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.SpeedWithinLimit`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `speed-within-limit` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
At, just below and just above 87 knots, and a groundspeed between 100 mph and 87 knots. Which printed figure governs is speed-limit's open question, reached through dependsOn.
```
