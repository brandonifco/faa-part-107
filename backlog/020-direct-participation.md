# direct-participation: A human being is directly participating in the operation

<!-- rules-factory-entry: direct-participation -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->

Backlog item 20 of 37 for FaaPart107, from RulesFactory.Maps.FaaPart107 4.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `direct-participation`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.39(a)
```

Evidence, verbatim:

```text
(a) That human being is directly participating in the operation of the small unmanned aircraft;
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
Part 107 does not define "directly participating", and the term carries the whole of the § 107.39 exception it states: whether the aircraft may be flown over a person turns on it. "In the operation of the small unmanned aircraft" states what the participation is in, not how direct it must be, and no determination is vested in anybody.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.DirectParticipation`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `direct-participation` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Gate 1 passes, gate 2 does not fire, and gate 3's first arm does not fire, so the procedure returns a gap. The same term delimits who must be briefed under § 107.49(b), and participant-briefing depends on this entry for that reason; the locator names § 107.39 alone because check-locators-section.py reads one section per citation, so an entry cannot cite two — a limit of the grammar rather than of the rule, stated where the claim is made. Compare § 107.39(b), whose "reasonable protection from a falling small unmanned aircraft" states its measure and is an assertion: two paragraphs of one sentence go opposite ways, and whose fact it is — a third party's, in both — is not what decides it. Found by 0010's re-derivation; trial 1's undefined-term sweep recorded this term against over-human-beings only and missed the § 107.49(b) instance.
```
