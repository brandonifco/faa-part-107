# over-human-beings: Operation over a human being

<!-- rules-factory-entry: over-human-beings -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->

Backlog item 21 of 37 for FaaPart107, from RulesFactory.Maps.FaaPart107 3.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `over-human-beings`
- kind: `operation`
- clarity: `clear`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.39
```

Evidence, verbatim:

```text
No person may operate a small unmanned aircraft over a human being unless— (a) That human being is directly participating in the operation of the small unmanned aircraft; (b) That human being is located under a covered structure or inside a stationary vehicle that can provide reasonable protection from a falling small unmanned aircraft; or (c) The operation meets the requirements of at least one of the operational categories specified in subpart D of this part.
```

## Dependencies

`dependsOn` -- built before this item:

- [`direct-participation`](020-direct-participation.md)
- [`reasonable-protection`](019-reasonable-protection.md)
- `subpart-d-categories` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- `waivable-regulations` -- no backlog item (scope: out -- the engine answers OutsideCurrentScope)

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] A test shows the rule does not apply while `waivable-regulations` holds.
- [ ] A hand-written handler implements `Handlers.OverHumanBeings`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `over-human-beings` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Three causes, three entries. The undefined term in (a) is direct-participation, a gap — the exception turns entirely on it and the corpus states no measure (0010). The judgement in (b) is reasonable-protection, an assertion. The deferral in (c) is subpart-d-categories, which is scope: out and carries OutsideCurrentScope itself — a dependency, not a second reason on this entry. The evidence is the whole section: the participating-person case, both limbs of (b) — a covered structure and a stationary vehicle — and the subpart D case, plus a plain overflight with none of them.
```
