# control-links-working: The control links are working properly

<!-- rules-factory-entry: control-links-working -->
<!-- rules-factory-engine: FaaPart107; map: RulesFactory.Maps.FaaPart107 -->

Backlog item 35 of 39 for FaaPart107, from RulesFactory.Maps.FaaPart107 2.0.0. Generated by `factory produce` from the map merged with `corpus-map.overlay.json`; change those, not this file.

## Entry

- id: `control-links-working`
- kind: `operation`
- clarity: `ambiguous`
- status: `mapped`

## Source

Locator, `sourceId` "cfr-14-107"; citation:

```text
§ 107.49(c)
```

Evidence, verbatim:

```text
(c) Ensure that all control links between ground control station and the small unmanned aircraft are working properly;
```

## Ambiguity

Fate: `unresolved`; unresolvedReason: `RequiresInterpretation`.

```text
Part 107 does not define "working properly", and the term carries the whole obligation. § 107.49(c) states no measure for it in its own constituent — "between ground control station and the small unmanned aircraft" identifies which links, not what properly is measured against — and vests the determination in nobody.
```

## Dependencies

`dependsOn` -- built before this item:

- none

## Reachability

`enabledBy` -- what the tests must set up:

- none

`suspendedBy` -- what the tests must show does not happen:

- none

## Acceptance criteria

- [ ] The engine performs the operation the evidence states, observable through its public API, on each situation the required evidence names.
- [ ] Where the answer turns on the question above, the engine declines with `RequiresInterpretation` citing this entry's locator; it does not choose a reading.
- [ ] A hand-written handler implements `Handlers.ControlLinksWorking`, the partial method `Generated/Contracts.g.cs` declares for this entry, with exactly its signature: once the entry is `implemented`, a missing or mis-typed handler does not build (#76).
- [ ] `corpus-map.overlay.json` sets `control-links-working` to `status: implemented`, with `implementedIn` and `tests`: every test named with its recorded `mutation` -- the change to the engine that turned that test red.
- [ ] After `factory produce` regenerates with that overlay, the generated correspondence tests stay green (`dotnet test`).

## Required evidence

From the entry's `note`:

```text
Gate 3 returns a gap, the same answer as effective-communication, prominent-objects and well-clear. That the remote pilot in command is the only person who could answer it is not gate 3's question (0008 § 2). Split out of preflight-actions (0010).
```
